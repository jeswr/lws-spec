\* THE FINDING — the refuted predecessor two-clocks text: enforcement lags
\* within a bounded interval while derived views reflect the record at once.
\* EXPECT VIOLATION of NoOracleWindow (trace: Create -> Propagate ->
\* RevokeImmediateAck — direct request honored, derived views already hide).
SPECIFICATION SplitClockSpec
CONSTANT Grants = {g1, g2}
CONSTANT Covering = {g1, g2}
CONSTANT Bound = 2
INVARIANT NoOracleWindow
