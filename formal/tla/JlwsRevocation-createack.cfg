\* The creation half of the same predecessor text: the create operation is
\* acknowledged before enforcement reflects the new grant — the agent told
\* "granted" is still refused by the lagging decision state.
\* EXPECT VIOLATION of AckedCreateReflected on SplitClockSpec (one step).
SPECIFICATION SplitClockSpec
CONSTANT Grants = {g1, g2}
CONSTANT Covering = {g1, g2}
CONSTANT Bound = 2
INVARIANT AckedCreateReflected
