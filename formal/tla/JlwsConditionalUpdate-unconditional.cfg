\* A server that accepts unconditional overwrites (what the MUST-428 rule
\* forbids) — EXPECT VIOLATION of NoLostUpdate (the classic race: both
\* writers read v1; one applies; the other blindly overwrites).
SPECIFICATION UnconditionalSpec
CONSTANT Writers = {w1, w2}
CONSTANT MaxVersion = 4
INVARIANT NoLostUpdate
